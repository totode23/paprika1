<div class="current-user">
  <% if user_signed_in? %>
    ユーザー名:<%= current_user.name %><br>
  <% else %>
    ログインしてください<br>
  <% end %>
</div>

<% if @foods.present? %>

  <div class="foods-index">
    <div class="food-image">
      <%= image_tag @foods.image , size: "180x110"%>
    </div>
    <%= check_box_tag %>
    <div class="food-name">
      <%= @foods.name %>
    </div>
    <div class="count-date">
      あと<%=@foods.expiry_at - Date.today %>日
    </div>
  </div>
  
<% else %>
  <div class="first-message-box">
    <div class="first-button">
      <%= button_to image_tag("register-icon.png"),{controller: "homes", action: "register"}, {method: :get, class: "button-register"}%>
    </div>
    <div class="first-message">
      <p>から食材を登録しましょう</p>
    </div>
  </div>
<% end %>